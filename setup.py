from setuptools import setup, find_packages

setup(
    name="pyxelpatch",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "pyxel",
    ],
    python_requires=">=3.7",
)
